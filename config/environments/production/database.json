{
  "defaultConnection": "default",
  "connections": {
    "default": {
      "connector": "strapi-connector-mongoose",
      "settings": {
        "client": "mongo",
        "uri": "${process.env.DATABASE_URI}"
      },
      "options": {
        "ssl": true
      }
    }
  }
}