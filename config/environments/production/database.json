{
  "defaultConnection": "default",
  "connections": {
    "default": {
      "connector": "strapi-hook-mongoose || strapi-connector-mongoose || mongoose",
      "settings": {
        "client": "mongo",
        "uri": "${process.env.DATABASE_URI}"
      },
      "options": {
        "ssl": true
      }
    }
  }
}